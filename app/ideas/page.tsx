import Link from 'next/link';import {InteriorPage} from '@/components/InteriorPage';import {ideas} from '@/content/seed';export const metadata={title:'Ideas'};export default function Ideas(){return <InteriorPage eyebrow="Ideas" title="Ideas worth using, questioning, and carrying forward."><p className="lead">Ideas are the durable center of DarrenDang.com. Books, writing, talks, and stories are different ways those ideas can travel.</p><div className="card-list">{ideas.map(i=><article className="content-card" key={i.slug}><div className="eyebrow bronze">{i.topic}</div><h2>{i.title}</h2><p>{i.summary}</p><Link className="text-link" href={`/ideas/${i.slug}/`}>Explore the idea →</Link></article>)}</div></InteriorPage>}
