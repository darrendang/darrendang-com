import {notFound} from 'next/navigation';import {InteriorPage} from '@/components/InteriorPage';import {ideas} from '@/content/seed';export function generateStaticParams(){return ideas.map(i=>({slug:i.slug}))}export default async function IdeaPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const idea=ideas.find(i=>i.slug===slug);if(!idea)notFound();return <InteriorPage eyebrow={idea.topic} title={idea.title}><p className="lead">{idea.summary}</p><h2>Put the idea to work</h2><p>This page is the beginning of a durable Idea record. It will grow with a real origin story, clear boundaries, practical applications, related writing, book chapters, talks, and connected ideas.</p><h2>Why this structure matters</h2><p>An Idea is not the same as an article or a quote. It is a reusable intellectual entity that can develop across multiple artifacts over time.</p></InteriorPage>}
