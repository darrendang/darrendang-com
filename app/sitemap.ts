import type {MetadataRoute} from 'next';import {ideas} from '@/content/seed';export default function sitemap():MetadataRoute.Sitemap{const base='https://darrendang.com';const routes=['','/ideas/','/writing/','/books/','/books/for-those-who-come-after-us/','/speaking/','/about/','/connect/','/privacy/','/accessibility/'];return [...routes.map(path=>({url:base+path,lastModified:new Date()})),...ideas.map(i=>({url:`${base}/ideas/${i.slug}/`,lastModified:new Date()}))]}
